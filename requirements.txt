# --- App-level deps -------------------------------------------------------
gradio>=6.0
huggingface_hub>=0.20.0

# --- SoulX-Singer inference deps ------------------------------------------
# Mirrors vendor/SoulX-Singer/requirements.txt minus preprocess-only packages
# (funasr / loralib / nemo_toolkit / sageattention / wandb) which are not
# imported by cli/ or soulxsinger/ — verified 2026-05-07.
accelerate==1.11.0
beartype==0.22.9
einops==0.8.2
g2p_en==2.1.0
g2pM==0.1.2.5
librosa==0.11.0
matplotlib==3.10.8
mido==1.3.3
ml_collections==1.1.0
nltk==3.9.2
numba==0.63.1
numpy<2.0.0
omegaconf==2.3.0
packaging==24.2
praat-parselmouth==0.4.7
pretty_midi==0.2.11
pyloudnorm==0.2.0
pyworld==0.3.5
rotary_embedding_torch==0.8.9
scikit_learn==1.7.2
scipy==1.15.3
six==1.17.0
setuptools==81.0.0
scikit_image==0.25.2
soundfile==0.13.1
ToJyutping==3.2.0
torch==2.2.0
torchaudio==2.2.0
torchcodec==0.10.0
tqdm==4.67.1
transformers==4.41.2
webrtcvad==2.0.10

# Run `python bootstrap_soulx.py` once after install to download the SoulX
# weights from Soul-AILab/SoulX-Singer and the NLTK corpora g2p_en needs.
# app.py invokes bootstrap automatically on startup, so this is a no-op
# on subsequent runs.
